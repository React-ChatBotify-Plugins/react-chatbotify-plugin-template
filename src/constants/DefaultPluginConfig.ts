/**
 * Default values for plugin config.
 */
export const DefaultPluginConfig = {
    autoConfig: true,
    promptColor: {
        "info": "blue",
        "warning": "yellow",
        "error": "red",
        "success": "green"
    }
}